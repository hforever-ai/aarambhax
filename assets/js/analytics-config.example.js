/**
 * Copy to analytics-config.js and set your IDs (or edit analytics-config.js in repo).
 * GA4: Admin → Data streams → Web → Measurement ID (G-XXXXXXXX).
 * Clarity: Project → Settings → Project ID.
 *
 * Leave empty string to disable that tool (safe for local dev).
 */
window.AARAMBHAX_GA4 = "";
window.AARAMBHAX_CLARITY = "";
